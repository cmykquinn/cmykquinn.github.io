---
---

	<nav class="pt-5 text-center bg-slate-900 bg-opacity-50 py-5">
      <a class="px-1 hover:underline sm:px-5" href="/">Home</a>
      <a class="px-1 hover:underline sm:px-5" href="/now">Now</a>
      <a class="font-bold" id="date-time" href="/"></a>
      <a class="px-1 hover:underline sm:px-5" href="/projects">Projects</a>
      <a class="px-1 hover:underline sm:px-5" href="/tunes">Music</a>
	</nav>

      <script>
      window.addEventListener("load", () => {
  clock();
  function clock() {
    const today = new Date();

    const hours = today.getHours();
    const minutes = today.getMinutes();

    const hour = hours < 10 ? "0" + hours : hours;
    const minute = minutes < 10 ? "0" + minutes : minutes;

    const hourTime = hour > 12 ? hour - 12 : hour;

    const time = hourTime + ":" + minute
    const dateTime = time;
    document.getElementById("date-time").innerHTML = dateTime;
    setTimeout(clock, 1000);
  }
});
    </script>